apply plugin: 'com.github.kt3k.coveralls'

coveralls {
    jacocoReportPath = "${rootProject.projectDir}/app/build/reports/kover/reports.xml"
}

tasks.coveralls {
    dependsOn 'koverMergedXmlReport'
}